# RhoHart Portfolio environment variables
#
# This project currently runs fully client-side with no required secrets.
# Add Vite-exposed variables here as the project grows (must be prefixed VITE_).
#
# VITE_CONTACT_ENDPOINT=
